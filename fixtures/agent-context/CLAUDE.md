# CLAUDE.md

Read `AGENTS.md` first. Treat this file as a minimal tool-specific adapter, not a second README.

## Working Rules

- Prefer small focused diffs.
- Run `npm run build`, `npm test`, and `npm run smoke` after changes.
- Use repo scripts instead of improvising when a script exists.
- Keep persistent rules short.
- Do not duplicate rules from `AGENTS.md`.
- Move long procedural workflows into `SKILL.md` fixtures or docs.
